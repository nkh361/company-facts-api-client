requests==2.31.0              # For handling HTTP requests to interact with the API
hashlib                       # Standard library for Proof-of-Work hashing (no need to install separately)
argparse                      # Standard library for command-line argument parsing (no need to install separately)
rich==13.7.0                  # For enhanced terminal output with colors and formatting
retrying==1.3.4               # For handling retries in case of failed API requests
flask==3.0.0                  # Web framework for building the API
flask-limiter==3.5.0          # Rate limiting for Flask
redis==5.0.1                  # Redis client for caching challenges
rich==13.7.0                  # For enhanced logging and terminal output
